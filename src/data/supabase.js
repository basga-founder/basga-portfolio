import { profile, skills, services, projects } from './portfolio'
const url='https://xjcilyiwkcamnfqkjgau.supabase.co', key='sb_publishable_NN46ZM5AxxaxVmuiLtDAvg_uPK4Pa08'
const headers={apikey:key,Authorization:`Bearer ${key}`}
export async function loadRemotePortfolio(){try{const r=await fetch(`${url}/rest/v1/portfolio_content?id=eq.1&select=data`,{headers});const [row]=await r.json();const d=row?.data;if(!d)return;if(d.profile)Object.assign(profile,d.profile);if(d.skills)skills.splice(0,skills.length,...d.skills);if(d.services)services.splice(0,services.length,...d.services);if(d.projects)projects.splice(0,projects.length,...d.projects)}catch(e){console.warn('Supabase unavailable',e)}}
export async function login(email,password){const r=await fetch(`${url}/auth/v1/token?grant_type=password`,{method:'POST',headers:{apikey:key,'Content-Type':'application/json'},body:JSON.stringify({email,password})});const d=await r.json();if(!r.ok)throw Error(d.error_description||'Login gagal');localStorage.setItem('basga_token',d.access_token)}
export async function saveRemotePortfolio(){const token=localStorage.getItem('basga_token');if(!token)throw Error('Login admin diperlukan');const data={profile,skills,services,projects};const r=await fetch(`${url}/rest/v1/portfolio_content?id=eq.1`,{method:'PATCH',headers:{apikey:key,Authorization:`Bearer ${token}`,'Content-Type':'application/json'},body:JSON.stringify({data})});if(!r.ok)throw Error('Gagal menyimpan ke Supabase')}